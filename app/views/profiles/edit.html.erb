<!-- app/views/profiles/edit.html.erb -->
<h1 class="text-2xl font-bold mb-4">Profile</h1>

<%= form_with model: @user, url: profile_path, method: :patch, class: "space-y-4" do |f| %>
  <div>
    <%= f.label :first_name, class: "block mb-1 font-semibold" %>
    <%= f.text_field :first_name, class: "w-full px-3 py-2 border rounded" %>
  </div>

  <div>
    <%= f.label :last_name, class: "block mb-1 font-semibold" %>
    <%= f.text_field :last_name, class: "w-full px-3 py-2 border rounded" %>
  </div>

  <div>
    <%= f.submit "Update Profile", class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700" %>
  </div>
<% end %>
